export interface SpawnPoint {
    x: number;
    y: number;
}

export interface EndPoint {
    x: number;
    y: number;
}

export interface MapPaths {
    spawnPoint: SpawnPoint;
    endPoint: EndPoint;
}
